pipeline {
    agent any

    environment {
        EMAIL_RECIPIENT = 'ajaypanand007@gmail.com'
    }

    stages {
        stage('Build') {
            steps {
                echo "Building the application using Maven"
            }
        }
        stage('Test') {
            steps {
                script {
                        echo "Running tests in JUnit"
                 }
            }
            post {
                always {
                    emailext (
                        to: "${env.EMAIL_RECIPIENT}",
                        subject: "Test Stage: ${currentBuild.currentResult}",
                        body: "The Test stage has ${currentBuild.currentResult}. Please find the attached logs.",
                        attachmentsPattern: '**/test-output.log',
                        attachLog: true
                    )
                }
            }
        }
        stage('Security Scan') {
            steps {
                script {
                        echo "Running security scan SonarQube"
                }
            }
            post {
                always {
                    emailext (
                        to: "${env.EMAIL_RECIPIENT}",
                        subject: "Security Scan Stage: ${currentBuild.currentResult}",
                        body: "The Security Scan stage has ${currentBuild.currentResult}. Please find the attached logs.",
                        attachmentsPattern: '**/security-scan.log',
                        attachLog: true
                    )
                }
            }
        }
        stage('Deploy') {
            steps {
                echo "Deploying the application in node"
            }
        }
    }
}
