pipeline {
    agent any
    stages {
        stage('Test Email') {
            steps {
                script {
                    emailext (
                        to: 'ajaypanand007@gmail.com',
                        subject: 'Jenkins Email Test',
                        body: 'Build finished. This is a pipeline test email from Jenkins.'
                    )
                }
            }
        }
    }
}
