pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the application'
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests'
            }
        }
        stage('Security Scan') {
            steps {
                echo 'Running security scan'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the application'
            }
        }
    }

post {
always {
        mail to: 'ajaypanand007@gmail.com',
             subject: "Basic Test Email",
             body: "This is a test email from Jenkins pipeline."
    }
}
}
